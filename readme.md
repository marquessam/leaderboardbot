# Select Start Leaderboard Bot

Discord bot for tracking RetroAchievements leaderboards.

## Setup
1. Clone the repository
2. Create a `.env` file with your Discord bot token:
3. Run `npm install`
4. Run `npm start`
